import { create } from '@storybook/theming';

export const theme = create({
	base: 'light',

	fontBase: '"Open Sans", sans-serif',

	brandTitle: 'dxTrade',
	// appContentBg: '#242729',
	// appBg: '#1c2237',

	textColor: '#000000',
});
