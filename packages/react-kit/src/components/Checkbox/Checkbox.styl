.container {
	display: inline-block;
	position: relative;

	.input {
		position: absolute;
		visibility: hidden;
	}

	.icon {
		display: block;
		margin: 0 auto;
		visibility: hidden;
	}

	.icon_isChecked {
		visibility: visible;
	}
	.view {
		display: inline-block;
		position: relative;
	}
}