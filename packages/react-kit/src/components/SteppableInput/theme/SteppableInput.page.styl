.customInput {
	padding: 0 6px;
	box-sizing: border-box;
	width: 100px;
	display: block;
	background-color: transparent;
	color: inherit;
	border: none;
	font-family: inherit;
	font-size: inherit;
	line-height: inherit;
	outline: none;
}