//@require '~config';

.container {
	position: absolute;
	box-sizing: border-box;
	visibility: hidden;
	z-index: -1;
}

.track {
	position: relative;
}

.bar {
	position: absolute;
	top: 0;
	left: 0;
	width: 100%;
	height: 100%;
}

.track {
	cursor: pointer;
}