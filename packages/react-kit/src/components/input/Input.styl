.container {
	outline: none;
	display: inline-block;
	box-sizing: border-box;
	position: relative;
}
	
.input {
	display: inline-block;
	outline: none;
	box-sizing: border-box;
	font-size: inherit;
	font-family: inherit;
	line-height: normal;
	color: inherit;
}