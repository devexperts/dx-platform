.container {
	position: absolute;
	top: 0;
	left: 0;
	bottom: 0;
	right: 0;
	background-color: inherit;
	display: flex;
	align-items: center;
	justify-content: center;
	visibility: hidden;
	opacity: 0;

	&_isVisible {
		visibility: visible;
		opacity: 1;
	}
}