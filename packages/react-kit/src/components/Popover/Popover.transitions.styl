$duration = 200;

.init {
    max-height: 0;
    transition: max-height ($duration)ms linear;
}

.exited,
.exiting {
    max-height: 0;
}

.entering,
.entered  {
    max-height: 100%;
}

:export {
    duration: $duration;
}